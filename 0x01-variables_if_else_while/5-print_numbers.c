/**
 * main - A  program that prints all single digit numbers
 * Return: 0 SUCCESSFUL MEG
 */

#include <stdio.h>

int main(void)
{
	int a;

	for (a = 0 ; a <= 9 ; a++)
	{
		printf("%d", a);
	}
	printf("\n");
	return (0);
}
