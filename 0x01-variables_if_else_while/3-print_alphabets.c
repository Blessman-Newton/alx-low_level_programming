/**
 *main - Enter
 *Return: Always
 */
#include <stdio.h>
int main(void)
{
char alpha;
for (alpha ='a' ; alpha <= 'z' ; alpha++)
putchar(alpha)
}
putchar('\n')
for (alpha = 'A' ; alpha <= 'Z' ; alpha++)
{
putchar(alpha)
}
putchar('\n');
return (0);
}
    
